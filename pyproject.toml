[project]
name = "sia"
version = "1.0.0"
description = "SIA (Scientia Intelligent Assistant) backend API and server"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.110.0",
    "uvicorn>=0.28.0",
    "requests>=2.31.0",
    "pydantic>=2.0.0",
]
